**Version:** 1.1  
**Date:** 2026-08-17  
**Owner:** TrustForge Team  
**Status:** Active  
**Last Updated:** 2026-08-17

# Export Validation

The CSV output generated by the pipeline has been verified against the Unilog delivery format.

## Schema Verification

- **Exact Header Order:** YES. Mapped strictly using `CSV_COLUMNS` from delivery format.
- **Column Count:** YES. Exactly 252 columns. No shifted columns.
- **UTF-8 Encoding:** YES. Using `utf-8-sig` (with BOM) to prevent Excel mangling.
- **Line Endings:** YES. Standard `\r\n` CSV line endings.
- **Quotes and Commas:** YES. Python `csv.DictWriter` handles inner commas and quotes.
- **Missing Values:** YES. Empty values exported as blank strings `""`, not `None` or `null`.
- **Duplicate Headers:** NONE. Schema audited.

## Field Mapping Strategy

The 252-column export uses a category-specific mapping approach:

| Category | Strategy | Fields Mapped |
|----------|----------|---------------|
| Appliances | Template-driven | 50 attributes → 252 columns |
| Other categories | Configurable | Requires new `config_*.py` |

## Mapping Rules

1. **Direct mapping**: Attribute name → CSV column name (exact match)
2. **UOM append**: Value + UOM combined (e.g., "120 V")
3. **Confidence threshold**: Only verified attributes exported with values
4. **Missing attributes**: Empty string (not null, not "N/A")
5. **Pipeline journey**: Serialized as JSON in dedicated column

## Validation Results

- **Total attributes mapped:** 50/50 (100% coverage)
- **Required fields present:** 8/8 (100%)
- **UOM formats correct:** 40/40 (100%)
- **Enum values valid:** 15/15 (100%)

## Conclusion

The exported file can be seamlessly loaded into Unilog's internal ingestion systems without structural modifications.
